apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secrets are provided by overlays (secretGenerator). Do not commit credentials.
resources:
  - cluster.yaml
  - backup-daily.yaml
  - restore-cronjob.yaml
  - restore-configmap.yaml
  - restore-rbac.yaml
  - backup-monitor.yaml
  - postgres-monitor.yaml
  - grafana-dashboard.yaml

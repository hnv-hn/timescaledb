apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: timescaledb
  labels:
    cnpg.io/cluster: timescaledb
    app.kubernetes.io/name: timescaledb
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: timescaledb
  podMetricsEndpoints:
    - port: metrics

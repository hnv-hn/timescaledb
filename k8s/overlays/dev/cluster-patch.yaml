apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: timescaledb
spec:
  instances: 1
  minSyncReplicas: 1
  maxSyncReplicas: 1
  storage:
    size: 10Gi
    storageClass: longhorn-1
  resources:
    requests:
      memory: '1Gi'
      cpu: '250m'
    limits:
      memory: '2Gi'
      cpu: '1'
  backup:
    retentionPolicy: '7d'
